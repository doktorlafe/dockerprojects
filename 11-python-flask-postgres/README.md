# 11 - Flask + PostgreSQL

Flask webová aplikace s PostgreSQL databází.

## Spuštění

```bash
cd 11-python-flask-postgres
docker-compose up
```

## Testování

```bash
curl http://localhost:5000/
curl http://localhost:5000/api/users
```

Databáze se automaticky vytvoří a propojí.
